<%= form_with model: @post do |f| %> 

<%= f.hidden_field :date %>
<div>
  <%= f.label :title %> 
  <%= f.text_field :title %>
</div>
<div> 
  <%= f.label :content %>
  <%= f.text_area :content %> 
</div>
  <%= f.submit 'Postar!' %>
<% end %> 